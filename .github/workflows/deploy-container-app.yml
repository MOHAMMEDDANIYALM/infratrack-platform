name: Deploy to Azure Container Apps

on:
  push:
    branches:
      - main

env:
  REGISTRY: infratrackacr.azurecr.io
  IMAGE_NAME: infratrack-platform

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Get ACR password
        id: acr_password
        run: |
          PASSWORD=$(az acr credential show --name infratrackacr --query "passwords[0].value" --output tsv)
          echo "password=$PASSWORD" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: infratrackacr
          password: ${{ steps.acr_password.outputs.password }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}

      - name: Deploy to Azure Container Apps
        uses: azure/container-apps-deploy-action@v1
        with:
          registryUrl: ${{ env.REGISTRY }}
          registryUsername: infratrackacr
          registryPassword: ${{ steps.acr_password.outputs.password }}
          containerAppName: infratrack-app
          resourceGroup: infratrack-rg
          imageToDeploy: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          location: centralindia
          containerAppEnvironment: infratrack-env
          yamlConfigPath: './containerapp-config.yaml'
      
      - name: Update Container App Configuration & Environment
        run: |
          # Apply full containerapp-config.yaml with port and health probe settings
          az containerapp update \
            --name infratrack-app \
            --resource-group infratrack-rg \
            --yaml containerapp-config.yaml \
            || echo "Note: YAML config may have applied partially"
          
          # Ensure critical environment variables are set
          az containerapp update \
            --name infratrack-app \
            --resource-group infratrack-rg \
            --set-env-vars \
              PORT=8080 \
              NODE_ENV=production \
              FRONTEND_URL=https://infratrack-app.graycoast-34d22b0c.centralindia.azurecontainerapps.io \
              JWT_SECRET=${{ secrets.JWT_SECRET || 'fallback-dev-secret' }} \
              JWT_REFRESH_SECRET=${{ secrets.JWT_REFRESH_SECRET || 'fallback-dev-refresh' }} \
              || echo "Env vars may already be configured"
